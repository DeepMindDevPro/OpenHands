# OpenHands 核心架构深度分析 & 一周速通方案

## 一、最强悍的6大设计模块

### 🏆 1. Agent 执行循环（Agent Step Loop）— 最核心的设计

**位置：** `Agent` + `LocalConversation`

这是整个系统的心脏，Agent 的 `step()` 方法实现了一个精巧的 8 阶段循环：

```
检查pending_actions → 检查Hook阻塞 → Condenser压缩 → LLM推理
→ 异常处理链 → 响应分类 → 并行工具执行 → Critic迭代精化
```

**强悍之处：**

- **异常处理链堪称教科书**：`FunctionCallValidationError` 反馈LLM继续、`LLMMalformedHistoryError` 触发恢复性压缩、`LLMContextWindowExceedError` 触发强制压缩——三层异常对应三种恢复策略
- **Critic 迭代精化**：Agent 完成后 Critic 打分，低于阈值自动重试+改进建议，形成闭环反馈
- **StuckDetector 5种循环模式检测**（重复动作/重复错误/独白/交替/上下文溢出），防止无限Token消耗

---

### 🏆 2. 工具系统（Action-Observation + 并行执行）— 最精巧的设计

**位置：** `ToolDefinition` + `ParallelToolExecutor`

**强悍之处：**

- **Action-Observation 统一协议**：所有工具交互遵循同一模式，LLM 只需理解一种交互协议
- **DeclaredResources 声明式并发控制**：工具声明资源依赖（如 `file:/path`），框架自动加锁——同文件串行、不同文件并行，在安全性和性能间取最优平衡
- **DiscriminatedUnionMixin**：`kind` 字段自动多态序列化/反序列化，新增工具零成本接入事件系统
- **MCP 动态适配**：`MCPToolDefinition` 从 JSON Schema 动态创建 Pydantic 模型，运行时无缝接入外部工具生态
- **安全增强**：导出给 LLM 的 Schema 中自动注入 `security_risk` 和 `summary` 字段

**工具分类总览：**

| 类别 | 工具 | 动作数 | 说明 |
|------|------|--------|------|
| 终端 | TerminalTool | 1 | Shell命令执行 |
| 文件编辑 | FileEditorTool | 5种命令 | view/create/str_replace/insert/undo_edit |
| 文件搜索 | GlobTool | 1 | Glob模式文件匹配 |
| 内容搜索 | GrepTool | 1 | 正则内容搜索 |
| 任务追踪 | TaskTrackerTool | 2种命令 | view/plan |
| 子代理 | TaskToolSet | 1 | 启动子代理任务 |
| 浏览器 | BrowserToolSet | 14 | 导航/点击/输入/截图等 |
| MCP | MCPToolDefinition | 动态 | 外部MCP服务器工具 |
| Gemini风格 | Edit/ReadFile/WriteFile/ListDirectory | 4 | Gemini模型优化 |
| 规划 | PlanningFileEditorTool | 5 | 仅可编辑PLAN.md |
| 内置 | Finish/Think/InvokeSkill | 3 | Agent必需 |

---

### 🏆 3. Condenser 上下文压缩（记忆系统）— 最优雅的设计

**位置：** `CondenserBase` 体系

6种压缩策略，解决 LLM 上下文窗口有限的核心痛点：

| 类型 | 机制 | 亮点 |
|------|------|------|
| llm | LLM 摘要压缩 | 独立 LLM 配置，可用更便宜模型 |
| amortized | 渐进遗忘 | 分摊压缩成本，keep_first 保护初始指令 |
| llm_attention | 注意力评分 | 精确选择重要上下文 |
| observation_masking | 遮蔽旧观察 | 隐私敏感场景 |
| recent | 保留最近N事件 | 极小上下文窗口 |
| noop | 不压缩 | 短对话 |

**强悍之处：** 压缩与 Agent 解耦，无需修改 Agent 代码即可切换策略；异常触发的强制压缩路径（上下文溢出→缩小窗口→再压缩）是生产级容错设计

**核心流程：**

```
Agent.step() 准备LLM消息
    → prepare_llm_messages(event_store)
        → Condenser.condense(events)
            ├─ 事件数 > max_size ? NO → 返回 View（继续LLM调用）
            └─ YES → LLMSummarizingCondenser
                1. 分割事件：keep_recent(最近N个) + to_summarize(旧事件)
                2. LLM生成摘要（独立LLM配置/模型，可用更便宜模型）
                3. 输出 CondensationSummary + keep_recent events
```

**异常触发的压缩路径：**
- `LLMContextWindowExceedError` → 强制压缩（忽略 max_size 检查，缩小 keep_recent 窗口）
- `LLMMalformedConversationHistoryError` → 恢复性压缩（修复损坏的历史）

---

### 🏆 4. 安全纵深防御（Security in Depth）— 最严谨的设计

**位置：** `SecurityAnalyzer` 体系

5层安全架构：

```
LLM层(预测security_risk) → Pattern层(正则匹配) → PolicyRail层(策略护栏)
→ Ensemble层(取最严结果) → Confirmation层(用户确认闸门)
```

**强悍之处：** fail-closed 设计（Ensemble 取最严结果）、ConfirmationPolicy 三级策略（NeverConfirm/ConfirmRisky/AlwaysConfirm）、安全评估与工具执行解耦

**SecurityAnalyzer 类层次：**

```
SecurityAnalyzerBase (ABC)
├── EnsembleSecurityAnalyzer  (组合多个分析器，取最严结果)
├── PatternSecurityAnalyzer   (正则模式匹配)
├── PolicyRailSecurityAnalyzer(策略护栏)
├── LLMSecurityAnalyzer       (LLM判断风险)
└── GraySwanSecurityAnalyzer  (灰天鹅检测)
```

---

### 🏆 5. Prompt 工程 — 最工程化的设计

**位置：** 系统提示模板 + Skills + Hooks + Plugin

OpenHands 的 Prompt 不是一段静态文本，而是一个**多层动态组装体系**：

- **SystemPromptEvent**：基础系统指令 + 工具 Schema + 动态上下文（secrets + agent_context）
- **Skills 多源注入**：5种来源（Public/User/Org/Project/Sandbox），通过 KeywordTrigger 和 TaskTrigger 动态触发，"后覆盖前"合并策略
- **Hook 拦截层**：UserPromptSubmit Hook 可在消息提交前拦截/修改/阻止，Hook 可拒绝 FINISHED 状态回退到 RUNNING
- **Plugin 参数注入**：Plugin 参数追加到 initial_message（而非系统提示），Agent 通过用户消息感知插件配置
- **Planning vs Default**：两种 Agent 使用完全不同的系统提示模板和工具集，Planning 明确声明"仅规划不实现"

**系统提示模板体系：**

| Agent类型 | 模板 | 约束 |
|-----------|------|------|
| Default Agent | `system_prompt.j2` | 全量工具（Terminal/FileEditor/Browser/SubAgents） |
| Planning Agent | `system_prompt_planning.j2` | 仅规划工具 + "仅负责规划，不负责实现" |

**Skill 多源加载（5种来源，按优先级合并）：**

| 来源 | 路径 | 说明 |
|------|------|------|
| Public Skills | OpenHands/skills GitHub仓库 | 社区共享技能 |
| User Skills | ~/.openhands/skills/ | 用户私有技能 |
| Organization Skills | {org}/.openhands 仓库 | 组织级技能 |
| Project Skills | .agents/skills/ / .openhands/microagents/ | 项目级技能 |
| Sandbox Skills | exposed_urls 配置 | 沙箱环境技能 |

---

### 🏆 6. 事件驱动架构（Event-Driven + Callback）— 最解耦的设计

**位置：** `EventStore` + `EventCallback`

**强悍之处：**

- **存储-回调分离**：EventService 负责持久化（FileSystem/S3/GCS），EventCallbackService 负责触发处理
- **DiscriminatedUnion 多态分发**：SetTitleCallbackProcessor / WebhookCallbackProcessor / Enterprise扩展（GitHub/Slack/Jira/GitLab/Bitbucket）
- **回调结果跟踪**：SUCCESS/FAILED/RETRY + 错误分类（budget_exceeded/model_error/runtime_error/timeout）

**事件类型层次：**

```
Event (基类)
├── LLMConvertibleEvent (可转换为LLM消息)
│   ├── SystemPromptEvent
│   ├── MessageEvent
│   ├── ActionEvent (工具调用)
│   ├── ObservationEvent (工具返回)
│   └── AgentErrorEvent
├── CondensationRequest
├── CondensationSummaryEvent
├── PauseEvent
├── TokenEvent
└── HookExecutionEvent
```

---

## 二、一周速通方案

### 方案A：架构优先路线（推荐，适合Java工程师转型）

**核心思路：** 先理解骨架，再填充血肉——从分层架构到核心循环，逐层深入

| 天数 | 主题 | 精读文件 | 目标 |
|------|------|----------|------|
| Day1 | 分层架构全貌 | openhands_architecture_analysis.md 全文 | 理解4层架构(AppServer→AgentServer→SDK→Sandbox)、包依赖、Namespace Package设计 |
| Day2 | Agent执行循环 | openhands/sdk/agent/agent.py + conversation/ | 画出 step() 的8阶段流程图，理解异常处理链和Critic机制 |
| Day3 | 工具系统 | openhands_tool_design.md + sdk/tool/ | 理解 Action-Observation 协议、DeclaredResources 并行控制、MCP 动态适配 |
| Day4 | 记忆+安全 | sdk/context/(Condenser) + sdk/security/ | 6种压缩策略对比、5层安全纵深防御、StuckDetector |
| Day5 | Prompt+Skill | skill_loader.py + hook_loader.py + Jinja2模板 | 多源Skill合并、Hook拦截、Plugin注入、Planning vs Default |
| Day6 | 事件+集成 | event/ + event_callback/ + integrations/ | 事件驱动架构、回调多态、Git Provider 集成策略模式 |
| Day7 | 串联+输出 | 画出完整架构图 + 写学习笔记 | 从用户请求到Agent执行的完整链路，标注关键设计决策 |

### 方案B：数据流驱动路线（适合喜欢追代码流的工程师）

**核心思路：** 跟踪一条用户消息从输入到输出的完整旅程，沿途学习每个模块

| 天数 | 追踪路线 | 关键入口 |
|------|----------|----------|
| Day1-2 | 会话启动全流程 | POST /app-conversations → LiveStatusAppConversationService._start_app_conversation() 8阶段 AsyncGenerator |
| Day3-4 | 消息执行全流程 | 用户消息 → Agent.step() → LLM推理 → 工具并行执行 → Observation → 事件推送 |
| Day5 | 上下文压缩流程 | prepare_llm_messages() → Condenser.condense() → LLM摘要 → View 过滤 → 异常触发压缩 |
| Day6 | 安全评估流程 | ActionEvent → LLM预测risk → Pattern匹配 → PolicyRail → Ensemble → Confirmation决策 |
| Day7 | 事件回调解耦 | Event产生 → EventStore持久化 → EventCallback触发 → Processor多态分发 → Webhook转发 |

### 方案C：对比学习路线（适合有Agent框架经验的工程师）

**核心思路：** 从设计模式角度对比 OpenHands 与其他框架，快速抓住差异化设计

| 天数 | 对比维度 | OpenHands 设计 | 学习重点 |
|------|----------|---------------|----------|
| Day1-2 | Agent 循环 | ReAct + Critic + StuckDetector 三重保障 | vs LangChain AgentExecutor 的单层循环 |
| Day3 | 工具系统 | Action-Observation + DeclaredResources + MCP | vs LangChain Tool 的简单串行执行 |
| Day4 | 记忆管理 | 6种Condenser + View + 异常触发压缩 | vs MemGPT 的分层记忆 |
| Day5 | 安全防御 | 5层纵深 + ConfirmationPolicy | vs 其他框架的"信任一切"默认策略 |
| Day6 | 多Agent | Planning→CodeAct 编排 + DelegateTool + ACP | vs AutoGen 的对话式多Agent |
| Day7 | 工程架构 | Namespace Package + Injector + DiscriminatedUnion | vs 单体框架的扩展方式 |

---

### 💡 高效学习技巧（三种方案通用）

1. **先读已有的3篇架构文档**：openhands_architecture_analysis.md（最全面）、openhands_architecture.md（最清晰）、openhands_systematic_learning_guide.md（最系统，含90+学习问题）
2. **画流程图而非读代码**：每个核心流程先画图，再对照源码验证
3. **关注 DiscriminatedUnionMixin**：这是整个系统的序列化基石，理解它就理解了事件/工具/回调的多态机制
4. **重点看测试**：tests/unit/app_server/ 中的测试用例是最好的行为文档
5. **跳过 Enterprise**：除非需要理解 SaaS 多租户，否则 Enterprise 代码可安全跳过
